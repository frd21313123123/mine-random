# SpeedrunPlugin

Плагин для Spigot/Paper серверов Minecraft 1.21 для проведения спидран-соревнований по поиску предметов.

## Как собрать

```bash
cd SpeedrunPlugin
mvn package
```

JAR файл будет в `target/SpeedrunPlugin-1.0.0.jar`

## Установка

1. Скопируйте `SpeedrunPlugin-1.0.0.jar` в папку `plugins/` вашего сервера
2. Перезапустите сервер

## Команды

| Команда | Описание | Разрешение |
|---------|----------|------------|
| `/speedrun <item_id|random> [modifier]` | Начать спидран | `speedrun.start` (op) |
| `/speedrunstop` | Остановить игру | `speedrun.stop` (op) |

## Модификаторы

Плагин поддерживает 1 модификатор на забег. По умолчанию он выбирается случайно (настраивается в `config.yml`).

Во время ивента доступен только хотбар (остальные слоты заблокированы и заполнены барьерами).

Доступные модификаторы:

- `gold` — разрешены только золотые инструменты (остальные инструменты удаляются)
- `veg` — можно есть только не-животную еду
- `time2` — смена дня и ночи в 2 раза быстрее
- `night` — бесконечная ночь
- `moon` — лунная гравитация (прыгучесть 2, предметы падают медленнее, плавное падение после приземления)
- `notrade` — нельзя торговать с жителями
- `randdrop` — редкий рандомный дроп при ломании любых блоков
- `aggro` — агрессивные животные (даже мирные атакуют игроков)

Пример:

```bash
/speedrun random moon
```

## Как использовать

1. Откройте сайт рандомайзера
2. Нажмите "ХАРДКОР" или "БЫСТРАЯ" чтобы выбрать предмет
3. Нажмите кнопку **🎮 /speedrun** чтобы скопировать команду
4. Вставьте команду в чат сервера

## Что происходит

1. **Подготовка** — все игроки телепортируются на стеклянные платформы в небе (Y=200)
2. **Восстановление** — HP, голод и сытость восстанавливаются, инвентарь очищается
3. **Время** — устанавливается утро (time set 0)
4. **Обратный отсчёт** — 10 секунд со звуком
5. **Старт** — платформы исчезают, игроки падают с защитой от урона
6. **Приземление** — защита от падения снимается
7. **Таймер** — отображается в action bar и в TAB-меню
8. **Победа** — первый игрок с предметом выигрывает + фейерверки

## Требования

- Minecraft 1.21
- Spigot или Paper сервер
- Java 17+
- Maven (для сборки)
